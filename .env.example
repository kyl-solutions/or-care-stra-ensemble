# Or-care-stra Ensemble Configuration

# Server
PORT=3457
NODE_ENV=development

# Database
DB_PATH=./database/ensemble.db

# Blockchain Configuration (Phase 3)
# BLOCKCHAIN_PROVIDER=local
# BLOCKCHAIN_RPC_URL=http://localhost:8545

# IoT Gateway Configuration (Phase 2)
# IOT_GATEWAY_HOST=localhost
# IOT_GATEWAY_PORT=1883
# IOT_GATEWAY_PROTOCOL=mqtt
